provider = "prime"
cli_version = "0.7.0"
captured = "2026-08-06"
cols = 100
rows = 10
expected_status = "idle"
expected_source = "pane_output"
origin = "harness-captured"
corroborators = ["← agents/resume", "Ctrl+O to expand"]
allow_other_providers = []
